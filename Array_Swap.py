if __name__ == '__main__':
    # numbers = [5, 3, 9, 0, 1]
    # cohort = ['we', 'fo', 're', 'go']
    # pop removes the value by its index
    # y = numbers.pop(1)
    # removes() the elements
    # z = numbers.remove(0)
    # print(y, z)
    # declaring the length of an array in python
    # x = len(numbers)
    # a = numbers.append(6 + 7)
    # D = numbers.append(6)
    # b = numbers.clear()
    # c = numbers.copy()
    # n = numbers.count(6)
    # numbers.extend(numbers + cohort)
    # print(a, D)
    # print(n)
    # print(numbers)

    # a = [1, 2, 3, 4, 5]
    # p = a[0]
    # Q = a[1]
    #
    # p, Q = Q, p
    #
    # print("The value of p after swapping", p)
    # print("The value of q after swapping", Q)

    a = 2
    b = 3
    c = 4
    temp = c
    c = a
    a = b
    b = temp
    print(a, temp, b, c)





